# Design System — Rende Mais

## Tokens NativeWind / Tailwind

Configure no `tailwind.config.js`:

```javascript
module.exports = {
  theme: {
    extend: {
      colors: {
        brand: {
          50:  '#F0FDF4',
          100: '#DCFCE7',
          200: '#BBF7D0',
          400: '#22C55E',
          500: '#16A34A',  // primária
          600: '#15803D',
          700: '#166534',
        },
        neutral: {
          50:  '#FAFAFA',
          100: '#F5F5F5',
          200: '#E5E5E5',
          300: '#D4D4D4',
          400: '#A3A3A3',
          500: '#737373',
          700: '#404040',
          900: '#171717',
          950: '#0A0A0A',
        },
        fgc: {
          badge: '#1D4ED8',
          light: '#EFF6FF',
        },
        warning: {
          DEFAULT: '#D97706',
          light: '#FFFBEB',
        },
        error: {
          DEFAULT: '#DC2626',
          light: '#FEF2F2',
        },
      },
      fontFamily: {
        inter:       ['Inter_400Regular'],
        'inter-500': ['Inter_500Medium'],
        'inter-600': ['Inter_600SemiBold'],
        'inter-700': ['Inter_700Bold'],
        mono:        ['JetBrainsMono_400Regular'],
        'mono-500':  ['JetBrainsMono_500Medium'],
        'mono-700':  ['JetBrainsMono_700Bold'],
      },
      borderRadius: {
        card:   '16px',
        button: '12px',
        pill:   '999px',
      },
    },
  },
}
```

## Componentes de Base

### Surface (card container)

```tsx
// Usar para todo card/container elevado
<View className="bg-white rounded-card p-4 shadow-sm" style={shadowLevel1}>
  {children}
</View>

// Versão destacada (melhor opção)
<View className="bg-white rounded-card p-4 border border-brand-200" style={shadowLevel2}>
  {children}
</View>
```

### Constantes de shadow (usar StyleSheet, não Tailwind)

```typescript
export const shadows = {
  level1: {
    shadowColor: '#000000',
    shadowOffset: { width: 0, height: 1 },
    shadowOpacity: 0.06,
    shadowRadius: 3,
    elevation: 2,
  },
  level2: {
    shadowColor: '#16A34A',
    shadowOffset: { width: 0, height: 2 },
    shadowOpacity: 0.12,
    shadowRadius: 8,
    elevation: 4,
  },
  level3: {
    shadowColor: '#000000',
    shadowOffset: { width: 0, height: -4 },
    shadowOpacity: 0.08,
    shadowRadius: 12,
    elevation: 8,
  },
}
```

## Escala Tipográfica — Classes NativeWind

```tsx
// Display XL — taxa principal
<Text className="font-mono-700 text-[32px] text-brand-500 leading-tight" />

// Display LG — cabeçalhos de tela
<Text className="font-inter-700 text-2xl text-neutral-950 leading-tight" />

// Display MD — seções
<Text className="font-inter-600 text-xl text-neutral-950" />

// Body LG — texto corrido principal
<Text className="font-inter text-base text-neutral-950" />

// Body MD — descrições, labels
<Text className="font-inter text-sm text-neutral-700" />

// Body SM — footnotes, timestamps
<Text className="font-inter text-xs text-neutral-400" />

// Mono XL — taxa CDI %
<Text className="font-mono-700 text-[28px] text-brand-500 leading-none" />

// Mono LG — projeção em BRL
<Text className="font-mono-500 text-xl text-neutral-950" />

// Mono MD — valores secundários
<Text className="font-inter text-sm text-neutral-700" />
```

## Expo Font Setup

```typescript
// app/_layout.tsx
import {
  useFonts,
  Inter_400Regular,
  Inter_500Medium,
  Inter_600SemiBold,
  Inter_700Bold,
} from '@expo-google-fonts/inter'
import {
  JetBrainsMono_400Regular,
  JetBrainsMono_500Medium,
  JetBrainsMono_700Bold,
} from '@expo-google-fonts/jetbrains-mono'
import * as SplashScreen from 'expo-splash-screen'

SplashScreen.preventAutoHideAsync()

export default function RootLayout() {
  const [loaded] = useFonts({
    Inter_400Regular,
    Inter_500Medium,
    Inter_600SemiBold,
    Inter_700Bold,
    JetBrainsMono_400Regular,
    JetBrainsMono_500Medium,
    JetBrainsMono_700Bold,
  })

  useEffect(() => {
    if (loaded) SplashScreen.hideAsync()
  }, [loaded])

  if (!loaded) return null
  // ...
}
```

## Status Bar

```typescript
// Sempre light mode: status bar dark (texto/ícones escuros sobre fundo claro)
<StatusBar style="dark" backgroundColor="#FAFAFA" />
```

## Splash Screen

```
Background: #FFFFFF
Logo: wordmark "rende mais" em Inter Bold, brand-500
Ícone: seta diagonal para cima (↗) em brand-500
Centralizado vertical e horizontal
```
